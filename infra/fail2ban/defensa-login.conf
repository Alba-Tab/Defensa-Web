[Definition]
# El servicio registra una línea por respuesta 401/403 del login administrativo.
failregex = ^\s*DEFENSA_LOGIN ip=<HOST> method=POST path=/api/auth/login status=(?:401|403)$
ignoreregex =
