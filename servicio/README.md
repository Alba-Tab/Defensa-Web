# Servicio de defensa

API y lógica de dominio de la plataforma. El modo simulado es el único implementado en esta
base. La configuración se recibe mediante variables `DEFENSA_*`; consulta `.env.example`.
